/**
 * Re-export of the shared Analysis Contract so server modules import from one
 * clean path (`./contract` / `../contract`) instead of reaching across folders.
 */
export * from "../../shared/analysisContract";
